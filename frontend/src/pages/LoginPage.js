import Login from "../Components/Login/Login";
import React from "react";

const LoginPage = () => {
  return (
    <React.Fragment>
      <Login />
    </React.Fragment>
  );
};

export default LoginPage;
